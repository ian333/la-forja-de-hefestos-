// Math pillar reuses the lab type shape from physics. Same status flags,
// same module/branch contract — only the content differs.
export type {
  Audience,
  ModuleStatus,
  PhysicsModule as LabModule,
  PhysicsBranch as LabBranch,
} from '@/physics/types';
